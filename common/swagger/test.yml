components:
  schemas:
    Test:
      type: object
      properties:
        results:
          type: array
          description: 결과 목록
          items:
            type: object
            properties:
              'type': 
                type: string
              title:
                type: string
              message:
                type: string
              create_datetime:
                type: datetime